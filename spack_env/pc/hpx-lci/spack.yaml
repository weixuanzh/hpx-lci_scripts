# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs:
  - octotiger@master ^hpx networking=lci,lcw,mpi ^openmpi@4.1.5 fabrics=ucx ^ ucx@1.14.0
  view: true
  concretizer:
    unify: true
  develop:
    hpx:
      path: ../../../../hpx
      spec: hpx@=master
    lci:
      path: ../../../../LC
      spec: lci@=master
    lcw:
      path: ../../../../lcw
      spec: lcw@=master
  repos:
  - /home/jiakuny/workspace/lcw/contrib/spack
